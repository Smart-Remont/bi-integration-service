services:
  backend:
    build:
      dockerfile: ./Dockerfile
      context: ./
    ports:
      - "9020:8000"
    env_file:
      - ./app/.env
    # volumes:
    #   - ./app:/app

    develop:
      watch:
        - action: sync+restart
          path: ./app
          target: /app
        - action: rebuild
          path: ./pyproject.toml